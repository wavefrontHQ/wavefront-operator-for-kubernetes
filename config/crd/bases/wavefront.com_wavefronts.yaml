---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.8.0
  creationTimestamp: null
  name: wavefronts.wavefront.com
spec:
  group: wavefront.com
  names:
    kind: Wavefront
    listKind: WavefrontList
    plural: wavefronts
    singular: wavefront
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Wavefront is the Schema for the wavefronts API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: WavefrontSpec defines the desired state of Wavefront
            properties:
              clusterName:
                description: ClusterName is a unique name for the Kubernetes cluster
                  to be identified via a metric tag on Wavefront.
                type: string
              collectorEnabled:
                description: CollectorEnabled is whether to enable the collector.
                type: boolean
              dataExport:
                description: DataExport options
                properties:
                  proxy:
                    description: Proxy configuration options
                    properties:
                      args:
                        description: Args is additional Wavefront proxy properties
                          to be passed as command line arguments in the --<property_name>
                          <value> format. Multiple properties can be specified.
                        type: string
                      deltaCounterPort:
                        description: DeltaCounterPort accumulates 1-minute delta counters
                          on Wavefront data format (usually 50000)
                        type: integer
                      enabled:
                        description: Enabled is whether to enable the wavefront proxy.
                        type: boolean
                      histogram:
                        description: Histogram distribution configuration
                        properties:
                          dayPort:
                            description: Port to accumulate 1-day based histograms
                              on Wavefront data format (usually 40003)
                            type: integer
                          hourPort:
                            description: Port to accumulate 1-hour based histograms
                              on Wavefront data format (usually 40002)
                            type: integer
                          minutePort:
                            description: Port to accumulate 1-minute based histograms
                              on Wavefront data format (usually 40001)
                            type: integer
                          port:
                            description: Port for histogram distribution format data
                              (usually 40000)
                            type: integer
                        type: object
                      httpProxy:
                        description: HttpProxy is used in configurations when direct
                          HTTP connections to Wavefront servers are not possible.
                        properties:
                          cacert:
                            description: Enable HTTP proxy with CA cert. Must be used
                              with httpProxy.host and httpProxy.port if set to true.
                            type: boolean
                          host:
                            description: Host must be used with httpProxy.port.
                            type: string
                          password:
                            description: When used with httpProxy.user, sets credentials
                              to use with the HTTP proxy if the proxy requires authentication.
                            type: string
                          port:
                            description: Port must be used with httpProxy.host.
                            type: integer
                          user:
                            description: When used with httpProxy.password, sets credentials
                              to use with the HTTP proxy if the proxy requires authentication.
                            type: string
                        type: object
                      metricPort:
                        description: MetricPort is the primary port for Wavefront
                          data format metrics. Defaults to 2878.
                        type: integer
                      tracing:
                        description: Distributed tracing configuration
                        properties:
                          jaeger:
                            description: Jaeger distributed tracing configurations
                            properties:
                              applicationName:
                                description: Custom application name for traces received
                                  on Jaeger's Http or Gprc port.
                                type: integer
                              gprcPort:
                                description: Port for Jaeger GRPC format data (usually
                                  14250)
                                type: integer
                              httpPort:
                                description: Port for Jaeger Thrift format data (usually
                                  30080)
                                type: integer
                              port:
                                description: Port for Jaeger format distributed tracing
                                  data (usually 30001)
                                type: integer
                            type: object
                          wavefront:
                            description: Wavefront distributed tracing configurations
                            properties:
                              port:
                                description: Port for distributed tracing data (usually
                                  30000)
                                type: integer
                              samplingDuration:
                                description: When set to greater than 0, spans that
                                  exceed this duration will force trace to be sampled
                                  (ms)
                                type: integer
                              samplingRate:
                                description: Distributed tracing data sampling rate
                                  (0 to 1)
                                type: integer
                            type: object
                          zipkin:
                            description: Zipkin distributed tracing configurations
                            properties:
                              applicationName:
                                description: Custom application name for traces received
                                  on Zipkin's port.
                                type: integer
                              port:
                                description: Port for Zipkin format distributed tracing
                                  data (usually 9411)
                                type: integer
                            type: object
                        type: object
                    type: object
                type: object
              metrics:
                description: Metrics has resource configuration for node- and cluster-deployed
                  collectors
                properties:
                  cluster:
                    description: Cluster is for resource configuration for the cluster
                      collector
                    properties:
                      resources:
                        description: Compute resources required by the Collector containers.
                        properties:
                          limits:
                            description: Limit CPU and Memory requirements
                            properties:
                              cpu:
                                description: CPU is for specifying CPU requirements
                                type: string
                              memory:
                                description: Memory is for specifying Memory requirements
                                type: string
                            type: object
                          requests:
                            description: Request CPU and Memory requirements
                            properties:
                              cpu:
                                description: CPU is for specifying CPU requirements
                                type: string
                              memory:
                                description: Memory is for specifying Memory requirements
                                type: string
                            type: object
                        type: object
                    type: object
                  collectorConfig:
                    description: Collector ConfigMap name. Leave blank to use defaults
                    type: string
                  node:
                    description: Node is for resource configuration for the node collector
                    properties:
                      resources:
                        description: Compute resources required by the Collector containers.
                        properties:
                          limits:
                            description: Limit CPU and Memory requirements
                            properties:
                              cpu:
                                description: CPU is for specifying CPU requirements
                                type: string
                              memory:
                                description: Memory is for specifying Memory requirements
                                type: string
                            type: object
                          requests:
                            description: Request CPU and Memory requirements
                            properties:
                              cpu:
                                description: CPU is for specifying CPU requirements
                                type: string
                              memory:
                                description: Memory is for specifying Memory requirements
                                type: string
                            type: object
                        type: object
                    type: object
                type: object
              proxyUrl:
                description: ProxyUrl is the proxy URL that the collector sends metrics
                  to.
                type: string
              wavefrontTokenSecret:
                description: WavefrontTokenSecret is the name of the secret that contains
                  a wavefront API Token.
                type: string
              wavefrontUrl:
                description: WavefrontUrl is the wavefront instance.
                type: string
            required:
            - collectorEnabled
            - wavefrontTokenSecret
            - wavefrontUrl
            type: object
          status:
            description: WavefrontStatus defines the observed state of Wavefront
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
